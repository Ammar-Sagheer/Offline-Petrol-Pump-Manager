node_modules/
.next/
out/
dist/
build/
.env
.env.local
*.log
.DS_Store

# Local dev-only Postgres data directory (mirrors the app-data folder used in
# production, kept out of git the same way).
.pgdata/

# Next 16 auto-generates these for AI coding agents on every `next dev` run.
AGENTS.md
CLAUDE.md
